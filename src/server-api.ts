/**
 * Rawfy — REST API server entry point
 *
 * Local HTTP API built on Hono.
 *
 * Endpoints:
 *   GET /fetch?url=<url>&format=markdown   — Fetch and return content
 *   GET /metadata?url=<url>                — Metadata only
 *   GET /health                            — Health check
 *   GET /version                           — Version info
 *
 * TODO: Phase 5 implementation
 */

// Placeholder to satisfy tsup build — will be replaced in Phase 5
console.error('rawfy: REST API server not yet implemented. Coming in Phase 5.')
process.exit(1)
