/**
 * Rawfy — Rawfy Markdown (RFM) serializer
 *
 * Produces the primary output format: YAML frontmatter + markdown body
 * + media descriptors + interactive elements section.
 *
 * See UI_UX.md § "output format design" for the full spec.
 *
 * TODO: Phase 4 implementation
 */

import type { PageData } from '../types.js'

/**
 * Serialize PageData into Rawfy Markdown (RFM) format.
 */
export function serializeRfm(_page: PageData): string {
  // TODO: implement in Phase 4
  return ''
}
