/**
 * Rawfy — MCP server entry point
 *
 * Exposes Rawfy as an MCP (Model Context Protocol) server.
 * Supports stdio transport (default) and SSE transport.
 *
 * Tools:
 *   rawfy_fetch     — Fetch a URL and return clean machine-readable content
 *   rawfy_metadata  — Extract only metadata from a URL (fast, no content)
 *
 * TODO: Phase 5 implementation
 */

// Placeholder to satisfy tsup build — will be replaced in Phase 5
console.error('rawfy: MCP server not yet implemented. Coming in Phase 5.')
process.exit(1)
